package org.zeki.myagend;

import javafx.application.Application;
import org.zeki.myagend.controller.scene.StartController;

public class Main {
    public static void main(String[] args) {
        Application.launch(StartController.class, args);
    }
}
